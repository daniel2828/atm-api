import { Injectable } from '@nestjs/common';

@Injectable()
export class AtmService {
  constructor() {}
  // async createCard(createCardDto: CreateCardDto): Promise<Card> {

  // }

  // async changePin(cardId: number, newPin: string): Promise<void> {

  // }
}
